{{#if data.title.show}}
<header class="journal-page-header">
  <h{{data.title.level}}>{{data.name}}</h{{data.title.level}}>
</header>
{{/if}}


<section class='journal-page-content progress-list-item'>
  {{> progressBoxes}}
  {{> rankPips}}
  {{> progressButtons}}

  <details>
    <pre><code>{{json this}}</code></pre>
  </details>
</section>
