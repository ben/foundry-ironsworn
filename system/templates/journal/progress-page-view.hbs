{{#if data.title.show}}
<header class="journal-page-header">
  <h{{data.title.level}}>{{data.name}}</h{{data.title.level}}>
</header>
{{/if}}


<section class='ironsworn journal-page-content progress-list-item flexcol' style="gap: 5px">
  <div class="flexrow nogrow" style="gap: 1em">
    {{> rankPips}}
    <h4 style="margin: 2px">{{currentRank}}</h4>
    {{> progressButtons}}
  </div>
  {{> progressBoxes}}

  <details>
    <pre><code>{{json this}}</code></pre>
  </details>
</section>
